[project]
name = "ria"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "andy miles", email = "andy@arrivingskills.ai" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ria = "ria:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
